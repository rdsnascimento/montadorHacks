/*
	Soma as medidas dos lados de um triangulo qualquer e retorna o perímetro
*/

	A <= 2 ;
	D <= D+A;

	A <= 2; 
	D <= D+A;

	A <= 3;
	D <= D+A;

	# resposta Final salva em memória
	mem[A] <= D ;

	# podia ser em qualquer posição, escolhi arbitrariamente a 3

	END;
	# comando especial de fim do programa
	# será 1010 repetido 4x, poderia ser qualquer instrução não definida; nos .pdf não há fim do programa (seria uma chamada ao O.S., mas isso cabe a outra matéria)

/*
	agora será montado para testeROM_01.asm
*/